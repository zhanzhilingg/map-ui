const PREFIX = 'map-ui-';
const FLEX = PREFIX + 'flex';
const GRID = PREFIX + 'grid';
const HIDDEN = PREFIX + 'hidden';
const SCROLLABLE = PREFIX + 'scrollable';
const RESIZABLE = PREFIX + 'resizable';
const READONLY = PREFIX + 'readonly';
const DISABLED = PREFIX + 'disabled';
const COLLAPSIBLE = PREFIX + 'collapsible';
const COLLAPSED = PREFIX + 'collapsed';
const FOCUS = PREFIX + 'focus';
const MULTIPLE_VALUES = PREFIX + 'multiple-values';
const ERROR = PREFIX + 'error';
const FLASH = 'flash';
const NOT_FLEXIBLE = PREFIX + 'not-flexible';
const DEFAULT_MOUSEDOWN = PREFIX + 'default-mousedown';
const FONT_REGULAR = 'font-regular';
const FONT_BOLD = 'font-bold';

export {
    PREFIX,
    FLEX,
    GRID,
    HIDDEN,
    SCROLLABLE,
    RESIZABLE,
    READONLY,
    DISABLED,
    COLLAPSIBLE,
    COLLAPSED,
    FOCUS,
    MULTIPLE_VALUES,
    ERROR,
    FLASH,
    NOT_FLEXIBLE,
    DEFAULT_MOUSEDOWN,
    FONT_REGULAR,
    FONT_BOLD
};
